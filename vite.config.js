import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
    // If you are deploying to GitHub Pages, set base to your repo name like:
    // base: '/your-repo-name/',
    // For Vercel or Netlify, you can usually leave it as '/'
    base: './',
})
